@model IEnumerable<Category>
@{
    ViewData["Title"] = "Index";
}



<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Category List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Category" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; Add Category
            </a>
        </div>
    </div>

    <br /><br />

<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Category Name
            </th>
            <th>
                Display Order
            </th>
            <th>
                Actions
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model)
        {
            <tr>
                <td width:50%>
                    @item.Name
                </td>
                <td width:30%>
                    @item.DisplayOrder
                </td>
                <td>
                    <div class="w-75 btn-group" role="group">
                            <a asp-controller="Category" asp-action="Edit" asp-route-id="@item.EncryptedId" class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i>Edit</a>
                            <a asp-controller="Category" asp-action="Delete" asp-route-id="@item.EncryptedId" class="btn btn-danger mx-2"> <i class="bi bi-trash"></i>Delete</a>
                    
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
</div>


@*<div class="row" style="margin-bottom:20px;">
    <div class="col-12">
        <form asp-action="Encrypt" method="post">
            <div class="input-group">
                <input type="text" name="data" id="data" class="form-control" />
                <span class="input-group-btn">
                    <input type="submit" value="Encrypt" class="btn btn-outline-primary" />
                </span>
            </div>
        </form>
    </div>
</div>

<div class="row">
    <div class="col-12">
        <form asp-action="Decrypt" method="post">
            <div class="input-group">
                <input type="text" name="data" id="data" class="form-control" />
                <span class="input-group-btn">
                    <input type="submit" value="Decrypt" class="btn btn-outline-primary" />
                </span>
            </div>
        </form>
    </div>
</div>

<hr />
Result: @TempData["Result"]*@
